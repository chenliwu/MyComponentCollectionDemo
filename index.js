/** @format */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';

import AppDraweNavigator from './src/AppDraweNavigator';

AppRegistry.registerComponent(appName, () => AppDraweNavigator);
